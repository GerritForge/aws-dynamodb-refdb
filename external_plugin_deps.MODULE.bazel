maven = use_extension("@rules_jvm_external//:extensions.bzl", "maven")

bazel_dep(name = "gerrit-aws-dynamodb-refdb")
local_path_override(
    module_name = "gerrit-aws-dynamodb-refdb",
    path = "plugins/aws-dynamodb-refdb",
)

use_repo(maven, "aws-dynamodb-refdb_plugin_deps")
